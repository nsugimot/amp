general:
  branches:
    only:
      - master

machine:
  ruby:
    version: 2.3.0

test:
  override:
    - 'true'

deployment:
  production:
    branch: master
    commands:
      - bundle exec middleman deploy
